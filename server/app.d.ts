import type { RequestListener } from 'node:http';

/** Express application (plain JS); typed as Node request listener for `http.createServer`. */
declare const app: RequestListener;
export default app;
